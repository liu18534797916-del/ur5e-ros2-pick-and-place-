Terminal  1
  cd ~/ros2_ws && source install/setup.bash && ros2 launch ur5e_gazebo_demo
  ur5e_sim.launch.py


Terminal  2
  cd ~/ros2_ws && source install/setup.bash && ros2 run ur5e_gazebo_demo
  pick_and_place_moveit
